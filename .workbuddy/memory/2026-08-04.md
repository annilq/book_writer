# 2026-08-04 工作日志

## BookCraft 项目架构分析
- 完成项目全景架构分析，涵盖架构设计、业务功能、交互体验、设计规范四个维度
- 分析了 5 层架构 (Presentation / Application / AI Engine / Data / External Services)
- 梳理了两种创作模式 (Autonomous 一键成书 vs Chat 对话式创作) 的完整流程与数据存储差异
- 分析了 Geist/Vercel 风格设计系统 (单色高对比 + 结构性边框 + brand Indigo 强调色)
- 报告输出至 `docs/architecture-analysis.md`
- 识别关键架构问题: fire-and-forget 可靠性、章节计数口径不一致、ClientContext SSR 守卫影响首屏

## /impeccable init 执行
- 调用 impeccable skill 的 init 命令，捕获产品真相到 PRODUCT.md
- 运行 context.mjs 确认项目无 PRODUCT.md，存在既有视觉实现 (Geist/Vercel 风格)
- 通过 AskUserQuestion 确认三个关键事实: 主用户=aspiring authors、产品性质=Commercial SaaS、定位=工作流+双模式控制
- 写入 /Users/yunqi/Documents/develop/book_writer/PRODUCT.md (platform=web, 双模式创作, 订阅制, 双语 zh/en, GitHub OAuth 唯一登录约束)
- init 仅写产品事实，视觉世界决策留给后续 new-work；未记录任何绑定视觉承诺

## /impeccable critique 跨界面总览
- 评审目标: 跨界面总览 (首页 → 工作台 → Agent)；目录级目标无稳定 slug，跳过快照持久化与趋势追踪
- 方法: 双子 agent 并行 (A 设计评审 agent-ad813bd9 · B 探测器 agent-e10caf6a)；本环境无浏览器自动化，B 仅 CLI 扫描 + 手动核查
- 探测器对 app/(75文件)/components/(42文件) 均返回 [] (exit 0，但 Tailwind 代码库下覆盖率仅约 1/3 规则，浏览器依赖规则未执行)
- Nielsen 10 项评分 **14/40 (Poor)**: 视觉基线合格，但交互契约不可信——Enter 发送失效、提交大纲裸 JSON.parse 无保护、三条静默失败路径拉低分数
- 设计专属性判定: 弱锚定，品牌色仅首页出现，工作台/聊天是通用 LLM 聊天，阅读视图不像书 (无章节标题/prose 未用/固定宽/低对比)
- 协议一致结论 (A+B 独立印证): Enter 发送失效机制、零 aria-label、对比度令牌数学 (success/destructive on /10 背景失败)、书架工具栏全 inert、破坏性操作无确认
- 优先级问题: P0 Enter 发送失效、P0 阅读视图不像书、P1 破坏性操作无确认/图标误导、P1 启动页让作者选 LLM、P2 终点无仪式
- 用户决策 (AskUserQuestion): 方向=交互可信度、范围=仅 Top 2 P0 (Enter 发送 + 阅读视图)、视觉=品牌色延伸入工作台
- 推荐行动: `/impeccable harden` (Enter 发送+流式停止) → `/impeccable typeset` (阅读视图成书+品牌色延伸) → polish → 重跑 critique
- 完整报告写入 `docs/critique-2026-08-04.md`
- 注: 破坏性操作无确认属 P1，是交互可信度主题下最高价值后续项，未计入本轮 Top 2 范围

## 执行 P0 修复 (用户: 一起修复p0)
- **P0 #1 Enter 发送失效**: `components/Chat/chat-box.tsx` 原 onKeyDown 调 `form.handleSubmit(onSubmit)` 丢弃返回处理器 → 改为 `form.handleSubmit(onSubmit)()`；发送按钮 size-6→size-9、加 aria-label={t("send")}、空输入/流式禁用；新增流式停止按钮 (Square 图标, onStop)；placeholder/aria 改 t("followUp")；恢复 textarea focus-visible:ring-1
- 两个父页面 `app/chats/[id]/page.client.tsx` 与 `app/content/[id]/page.client.tsx` 从 useChat 解构 `stop` 并传 `onStop={stop}` (标准 @ai-sdk/react v4 API)
- i18n 新增键: send / followUp / stop
- **P0 #2 阅读视图成书**: `app/books/[id]/components/content.tsx` 渲染章节标题 (position+title 为 h2, 用 --brand 标记序号)、仅 content 存在时渲染 Markdown、null 跳过；`app/books/[id]/page.client.tsx` 阅读容器 w-[1000px] bg-secondary → w-full max-w-3xl mx-auto px-6 py-10 内嵌 prose、空章节显示空状态+CTA、Header 加导出 .md 按钮；`app/books/[id]/components/outline-sheet.tsx` FAB 加 aria-label + 品牌色；`app/globals.css` 加 `.prose p{@apply text-foreground}` 覆盖全局 muted-p 保证阅读对比度
- i18n 新增键: exportBook / emptyBook / viewChapters
- 品牌色(--brand) 按用户决策延伸到工作台: 章节序号标记、大纲 FAB、导出按钮
- 验证限制: 本环境未安装 node_modules (find 被 OOM 杀掉)，无法跑 next build/tsc；仅做了 i18n JSON 合法性校验通过。建议用户本地 pnpm install && pnpm build 验证
- 注意: 破坏性操作无确认 (P1) 与启动页选 LLM (P1) 未在本轮处理，是后续 hardening/onboard 范围

## 执行 P1 收尾 + P2 修复 (用户: 接着修p1 → 修改p2)
- **P1 #3 章节再生按钮**: `app/content/[id]/components/outline.tsx` 误标的 `<Play>` 改为 `<RotateCcw>` (再生语义)，并用 AlertDialog 包裹 `clearMessageOfChapter` 的破坏性调用；新增 i18n 键 regenChapterTitle/regenChapterContent/regenerate
- **P1 #4 编辑消息硬删下游加确认**: `components/Chat/chat-box.tsx` 编辑模式提交走 `onInputMessage` 会经 `removeMessagesAfterMessageId` 静默删下游；onSubmit 时若处编辑模式先弹 AlertDialog 确认，确认后才触发；新增 i18n 键 editDiscardTitle/editDiscardContent/editDiscardAction
- **P1 #5 大纲 JSON.parse 保护 (两处都补完)**:
  - `app/content/[id]/components/chapter-content.tsx` 补 `import { toast } from "@/hooks/use-toast"` (上一轮 try/catch 已加但漏 import，会导致构建破裂 → 已修正)；畸形 JSON 时 toast jsonParseError
  - `app/chats/[id]/components/book-viewer-layout.tsx` 补 toast import + try/catch 包裹 `JSON.parse(app.code)`，失败时 toast 错误并调用既有 `onRequestFix(message.content)` 修复
- **P2 #7 AgentProgress 等待安抚 + 防冻结**: `app/books/[id]/agent/AgentProgress.tsx` 重写——`load()` 包 try/catch，fetch 失败进入 reconnecting 状态并限次(8)重试避免永久冻结；状态区加 `aria-live=polite` (stepper/状态/日志三处)；RUNNING 加安抚文案 `agentReassure` (可关闭页面/进度自动保存)；DONE 横幅加 `animate-in fade-in-0 zoom-in-95` 克制庆祝；DONE 主 CTA 改指向阅读视图 `/books/${bookId}`；新增 i18n 键 agentReassure/agentReconnecting
- **P2 #8 章保存内联确认 + 整书完成庆祝**: `app/content/[id]/page.client.tsx` 重写 `onSave`——非 COMPLETE 不再 `setMessages([])` 空白屏，改为 toast `chapterSaved` + `setActiveMessage(undefined)` 折叠面板回到对话(进阶下一步)；COMPLETE 时弹出整屏庆祝浮层 (brand 点缀 + `bookCompleteTitle/Desc` + `readBook` CTA 指向 `/books/${chat.id}`)；移除已不用的 `ToastAction` import；新增 i18n 键 chapterSaved/bookCompleteTitle/bookCompleteDesc/readBook
- i18n zh/en 两个 JSON 均通过合法性校验
- **待办**: P1 #6 启动页上手优化 (model 改可选+预选推荐、友好模型名、每字段 FormLabel、模式选中态增强、高级选项折叠、SUGGESTED_PROMPTS 改 render 时 t() 求值) 仍未执行，是最后一个未完成的 P1 项
- 验证限制同前: 本环境无 node_modules，无法跑 build/tsc；建议用户本地 `pnpm install && pnpm build` 后走查 (P2 庆祝浮层/aria-live/防冻结、P1 确认弹窗/JSON 保护)

## /impeccable polish 质量精炼 (16:29)
- 对 P0/P1/P2 全部改动做最终精炼：探测器 `app/`+`components/` 均 `[]`；i18n JSON 合法；无残留未用导入
- 修复 4 处真实缺陷: (HIGH) 整书完成庆祝浮层原只能导航退出→加背景点击关闭+右上角 X 关闭按钮(aria-label=close); (MED) reconnecting 横幅硬编码 amber→改用语义 --warning 令牌; (LOW) 发送按钮流式时禁用态+Spinner 与停止按钮重复→改始终显示箭头并移除 Spinner 导入; (LOW) 移除 outline-sheet 遗留 console.log(nodeEl)
- 确认无回归: 阅读视图 prose 对比度修复(.prose p 覆盖全局 muted-p)、Enter 发送、流式停止、破坏性操作确认/JSON.parse 保护、品牌色延伸至阅读/工作台 均完好
- 报告写入 `docs/polish-2026-08-04.md`；i18n 新增 `close` 键(中:关闭/英:Close)
- 建议下一步: 跑 `/impeccable critique` 重评看分数提升；最后处理 P1 #6 启动页

## /impeccable critique 重评 (16:32)
- 在 P0/P1/P2+polish 全部落地后重评。方法: 双子 agent 并行 (A agent-86b6a06c 设计评审 · B agent-40e595b7 探测器)；目录级目标无稳定 slug，跳过快照持久化/趋势
- B 探测器对 app/ 与 components/ 仍 `[]`，但**覆盖率仅约 1/86 文件**——引擎对 Tailwind 工具类/JSX inline style 盲，exit 0 = "未测量"非"无缺陷"；B 改用令牌数学+手动核查补强
- **设计健康分 21/40 (Fair)，较首次 14/40 提升 +7**。增益精确落在修复落点: H1+2, H3+2, H5+1, H9+1；天花板被零修复的启动页压住
- **设计专属性仍弱**: 仅阅读视图有「书」的形态，其余是通用 LLM 聊天；品牌色旅行了(--brand 延伸到阅读/工作台)但品牌色≠专属性；发现系统级不一致: `--primary`(Geist 单色) 与 `--brand`(靛蓝) 同时用于各界面最重要操作
- **本轮修复引入/遗留的真实缺陷(诚实项)**:
  1. token 对比度仍失败: text-warning on bg-warning/10=1.98:1 FAIL(light)、text-success on /10=2.61:1 FAIL、dark text-destructive=1.92:1 FAIL → polish 把硬编码 amber 换 --warning 令牌但令牌本身亮度未验证=假安全感
  2. 庆祝浮层无键盘关闭/无 role=dialog/aria-modal(只加了 X 按钮+背景点击，半截)
  3. AgentProgress 重连耗尽(8次)后静默卡死: status 仍 RUNNING、Loader 永远转、无错误无重试入口——防冻结修复的半截
  4. 阅读视图无 <h1>(书标题是 span、空状态标题是 p、章节从 h2 起)
  5. chat-box 编辑取消 X 按钮无 aria-label；setValue(undefined) 吞草稿
- **剩余优先级问题**: P0 启动页逼作者选 LLM(且被复制到 BookDialog，label* 键已存在无人用)；P0 静默失效控件5处(Settings onSubmit=console.log、FormMessage 被 textarea 盖住、onError=console.log、书架工具条 inert、Sidebar href=#)；P1 无障碍仅示范非系统(6 aria-label vs 16+ 无名图标按钮、对比度令牌失败、lang=en 静态、tree ARIA 陷阱)；P1 进度/状态讲错故事(chaptersTotal 计父节点、刷新后 chapter=null、空状态 CTA 标 chat 指 agent)；P2 聊天无作者声音+死重(chat-log 同排版、RefreashMessage 裸调销毁、chapter-content 死代码、Reasoning div onClick)；P3 docs 页零引用可达
- 报告写入 `docs/critique-rerun-2026-08-04.md`
- 验证限制同前: 本环境无 node_modules 无法 build/tsc；B 确认 ENTER 修复/Stop/AlertDialog/JSON try-catch/prose 对比度均正确落地；新发现的对比度/键盘关闭/重连卡死/无 h1 需本地浏览器走查确认

## /to-spec 把重评剩余问题合成可执行 spec (16:58)
- 用户执行 `/to-spec 处理上面的问题`：把 critique 重评的全部剩余问题(P0 启动页/P0 静默失效控件/P1 无障碍系统/P1 进度状态/P2 聊天作者声音/P3 帮助可达 + 引入的缺陷)合成为一份 `ready-for-agent` 规格
- 输出文件 `docs/spec-ux-hardening-2026-08-04.md`，含 Problem Statement / Solution / 34 条 User Stories / Implementation Decisions / Testing Decisions / Out of Scope / Further Notes
- 采用 PRODUCT.md 领域术语(Book/Chapter/Message/AgentRun/dual-mode/outline tree/reading view)；复用既有 i18n 键(label*)而非新建；标注 `--primary` 与 `--brand` 双主色冲突待产品决策
- **发布约束**: to-spec 要求 publish 到 issue tracker 并打 `ready-for-agent` triage 标签，但本环境无 `gh` 且所有 connector 断开、未跑 `/setup-matt-pocock-skills` → 无法推到外部 tracker，已作为本地交付并注明发布 gap
- 测试 seam 锚点: 进度计算抽纯函数(vitest node 环境, 复用 `utils/agent/outline.test.ts` 先例)、表单提交契约、章节 store 水合、状态色对比度(node 计算)、破坏性操作 seam 审计、庆祝浮层 dialog 语义、帮助可达性；DOM 测试需补 jsdom 环境(标注为前置)
- 修复范围优先级(供后续执行): 单组件 BookOutlineForm 一次修好前门/创建流/设置面；破坏性调用点按 operation 而非 symptom 审计；muted-by-default 全局 p 规则是否为正确默认待设计 owner 决定

## /to-tickets 把 spec 拆成 13 张 tracer-bullet ticket (17:03)
- 用户执行 `/to-tickets 将上面的问题生成ticket`：基于 `docs/spec-ux-hardening-2026-08-04.md` 拆成 13 张垂直切片 ticket，每张横切各层、可独立验证、声明阻塞边、标记 ready-for-agent
- 经 AskUserQuestion 确认粒度与阻塞关系 OK（q-0 直接生成 13 张）；按依赖顺序(阻塞者在前)写入本地 `.scratch/ux-hardening/issues/<NN>-<slug>.md`
- 13 张: 01 状态色令牌对比度(None) / 02 启动表单作者化(None) / 03 设置持久化(←02) / 04 生成错误可见(None) / 05 消除死控件(None) / 06 统一破坏性确认seam(None) / 07 可信自主进度+章节恢复+CTA对齐(None,含抽纯函数) / 08 完成庆祝modal语义(None) / 09 AgentProgress重连失败终态(None) / 10 阅读视图h1(None) / 11 图标按钮aria-label+动态lang(None) / 12 大纲树键盘+reasoning披露(None) / 13 帮助页导航可达(None)
- 唯一真实阻塞边: 03→02（设置面板复用重做后的共享表单，先重做再接持久化）；其余为独立 UI 修复故并行，符合实际
- 发布约束同 to-spec: 无 gh/未配 tracker，故写本地文件而非推外部 issue tracker；文件遵循本地模板(What to build/Blocked by/ready-for-agent/验收清单)，不含具体文件路径
- 用户可后续配 `/setup-matt-pocock-skills` 后把本地 ticket 推到真实 tracker（原生 blocking 关系或文本 Blocked by）

## /implement 03 设置持久化落地 (17:32)
- 用户执行 `/implement 03`。调用 implement skill（基于 spec/ticket 落地 + 跑检查 + /code-review + 提交）。
- 根因: SettingsModal 的 Save 仅 `console.log(data)` 且按钮被 `<SheetClose asChild>` 包住 → 点 Save 关面板但不写库 = 假保存。
- 修复:
  1. `app/api/chat/actions.ts` 新增 `updateBookMeta(bookId, {title?,description?,model?,prompt?,categories?})` 服务端 action，部分更新；categories 用 `set:[{name}]` 且仅当非空（与现有 createBook 同库、同无显式 auth 风格）。
  2. `app/chats/[id]/components/setting-modal.tsx`：onSubmit 调 updateBookMeta；成功 toast `bookSaved` + 仅成功才 `setOpen(false)`；失败 toast `bookSaveFailed`；移除 SheetClose 包裹的假保存 antipattern；加 `saving` 态 + Loader2；四个字段补 `FormLabel`(labelTitle/labelDescription/labelCategory/labelModel)，model 渲染改 `{name} · {provider}` + 推荐标记（继承 ticket 02 作者化样式）；Select 改受控 `value={field.value}`；删未用 `SheetClose` 导入（SheetContent 自带 X 关闭）。
  3. i18n: en/zh 各加 `bookSaved`/`bookSaveFailed`。
- 验收: 4 项清单全满足（持久化存活、Save 不再=关闭即保存、失败可见、继承作者化标签）。
- 限制: 本环境无 node_modules 无法 tsc/vitest；已做 JSON 合法性 + 括号配平 + 静态自审（调用点唯一、类型匹配、与 createBook/updateMessage 风格一致）。`/code-review` 斜杠命令本模型不可直接调用，已做等价人工评审。
- 提交: `ca5ca36` (HEAD dev)，仅含 03 相关 4 文件（未混入其他 ticket 的未提交改动）。
- 安全备注: updateBookMeta 与既有 chat actions 一致，未加 ownership 校验（依赖页面层 session 守卫）；若后续要收紧可在此补 `auth()` + 归属判断。

## /code-review 双轴审查 UX hardening 全量改动 (17:50)
- `code-review` skill 实际存在于 `~/.workbuddy/skills/code-review/`（frontmatter `disable: true`，但用户显式 `/code-review` 可用）。**上一轮误判为"斜杠命令不可调用"，实为未去 skills 目录查找——以后先 `ls ~/.workbuddy/skills/`**。
- 基准点 `origin/dev`=c42c0b3，merge-base 相同；**绝大部分工作未提交，故 diff 命令必须用 `git diff origin/dev`（工作树）而非 `origin/dev...HEAD`**（后者只有 ca5ca36 一个提交）。另需单独读未跟踪源文件 `components/LocaleLang.tsx`、`utils/theme-contrast.test.ts`。
- 仓库无 CODING_STANDARDS/CONTRIBUTING/AGENTS.md → Standards 轴用 Fowler smell baseline + 从邻近文件反推的仓库约定；工具链已有 next lint/prettier/vitest（其覆盖项跳过）。
- 双 agent 并行: Standards(agent-3b071b5c) · Spec(agent-8aaaf9c2)。两轴独立命中同一批要害，可信度高。
- **两个已亲自核实的编译/运行时崩溃**（`app/(main)/components/BookOutlineForm.tsx`）:
  1. `FormLabel` 用了 5 次但第 16 行导入清单里没有 → 编译失败。
  2. `useEffect`(92) 依赖数组 `[models, form]`(96) 引用第 172 行才声明的 `const form` → TDZ ReferenceError，首页/书架弹窗必崩。
  3. 示例填充展开顺序反了 `{...data, ...form.getValues()}`(334)，空串盖掉示例值 → 点示例等于什么都不填。
- **Spec 轴逐票裁决**: 01 DONE；02 WRONG；03/06/08/10/11/12 PARTIAL；04/05/07/09/13 NOT DONE。即 13 张里仅 1 张真正完成。
- **系统性教训**: 本轮先批量往 en/zh 两份 JSON 加 i18n 键、再改代码，结果 9 个键（generationFailed/newBook/gridView/listView/prevVersion/nextVersion/back/help/agentReconnectFailed）成为零引用死键——**i18n 键应在消费它的那次编辑里同步添加，不要预先铺**。`utils/constants.ts` 删第三条示例后 `bookTitle3/bookDesc3` 也成孤儿未清。
- **越界改动（未在任何 ticket 中要求）**: Markdown 导出按钮、停止生成按钮与 stop 透传、发送按钮 canSend 禁用+尺寸 6→9、大纲 JSON 解析失败 toast、全局 `.prose` 覆写、`content.tsx` 整体重写、书籍页头部重排、OutlineSheet 触发器改品牌色。
- **确认封套仍有漏口**: `Refreash.tsx` 下拉内 retry 按钮 `refresh(book!.model)` 绕过新加的 AlertDialog 直达 `removeMessagesAfterMessageId`；ticket 06 审计条未满足。
- 破坏性确认在本次 diff 里被手搓三遍且实现各异（chat-box/Refreash/outline）→ 应抽 `<ConfirmDialog>`/`useConfirm` 再收口。

## 执行剩余无依赖 ticket 07-12 (续 02/04/05/06/13 之后)
- 本批补齐 6 张: 07 真实进度、08 完成庆祝弹窗语义、09 重连失败终态、10 阅读视图 h1、11 余下 aria-label、12 大纲树键盘。02/04/05/06/13 已于前序批次落地（BookOutlineForm 已作者化、ConfirmDialog 已统一、生成错误 toast、Help 入导航、死控件已接）。
- **07 真实进度**: `Chapter.leaf` 是真相分母（runner 只给 leaf 写 content）。新增 `utils/agent/progress.ts` 纯函数 `computeChapterProgress(chapters)→{done,total,pct,isComplete}`（仅计 leaf），配 `utils/agent/progress.test.ts`（vitest node 环境，覆盖"卡 64% 但 DONE"回归：9 leaf+5 parent → 9/9=100%）。`api/book/[id]/agent/route.ts` 改用 `computeChapterProgress` 给 `chaptersTotal/chaptersDone`，不再 `chapters.length`。`app/content/[id]/page.client.tsx` 新增挂载时从 `book.currentChapterId` 注水 chapter store，修复刷新后 chapter 为 null 导致 appendMessage 传 undefined。
- **08 庆祝弹窗**: `app/content/[id]/page.client.tsx` 给庆祝遮罩加 `role="dialog" aria-modal="true" aria-labelledby`，加 `useEffect` 实现 Escape 关闭、焦点陷阱（Tab 循环）、关闭后焦点归还触发元素（捕获 document.activeElement）。
- **09 重连失败终态**: `AgentProgress.tsx` 原 catch 超过 `MAX_RECONNECT_ATTEMPTS` 后返回 null → 轮询停但停留在陈旧 spinner（静默冻屏）。改为 `setReconnectFailed(true)` 并返回 `"RECONNECT_FAILED"`；effect 在不轮询该态；渲染前插一个终态面板渲染 `agentReconnectFailed` 键 + 手动重试按钮；`load` 返回类型补 `"RECONNECT_FAILED"`。`retry` 重置 `reconnectFailed`。
- **10 阅读视图 h1**: `app/books/[id]/page.client.tsx` 空状态 `<p>`→`<h1>`；有章节时在 prose 顶部加 `<h1>{book.title}</h1>`（章节为 h2）。头部面包屑的 title 保持 `<span>`（那是 chrome，不应是文档标题，避免双 h1）。
- **11 余下 aria-label**: `app/explore/page.tsx` 给搜索 Input 加 `aria-label={t("search")}`、Grid 图标按钮加 `aria-label={t("gridView")}`；`app/content/[id]/page.client.tsx` 给返回/关闭(X) 按钮补 `aria-label={t("back")}`/`t("close")}`。`app/chats/[id]/page.client.tsx` 的 back/close 已在前批补；动态 lang（`LocaleLang`）已就绪。
- **12 大纲树键盘**: 先 WebFetch 确认 react-arborist 已自带 `role=tree/treeitem`、`aria-expanded/selected` 与方向键键盘导航——故**不**在自定义 Node 上重复加 role（会生成嵌套 treeitem 非法）。改为给 `components/tree/index.tsx` 的 Node `<div>` 加 `tabIndex={0}` + `onKeyDown`（Enter/Space 与 Arrow 展开/折叠内部节点）+ `aria-*` + `focus-visible` 焦点环，作为库键盘能力的显式兜底，且不破坏库的结构语义。
- 验证（无 node_modules，仅静态）: i18n 字面键 107 个，EN/ZH 零缺失（5 个"缺失"是误报——正则匹配到 `searchParams.get("success")`、`createElement("a")` 等子串）；编辑文件括号配平全部 OK；`load` 返回类型已补 `"RECONNECT_FAILED"`。
- 待办: 全量未提交工作树建议提交（仅 stage `app components utils store`，排除 `.workbuddy/`/`.scratch/`/lockfile/PM 切换/分析文档）。BookOutlineForm 的 `model: z.string().min(1)` 仍强制，但 useEffect 已预选首模型，实践中非空——若要"真正可选"需放宽 zod，未在本批处理。
